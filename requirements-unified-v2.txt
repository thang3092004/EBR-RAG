-r requirements.txt

# Unified V2 train-free processing
opencv-python-headless>=4.10
scenedetect[opencv]>=0.6.4
open_clip_torch>=2.26
spacy>=3.7,<3.9
pyannote.audio==3.1.1
Pillow>=10

# Optional OCR is deliberately not installed here. PaddleOCR needs a
# PaddlePaddle build selected for the server's CUDA version.
